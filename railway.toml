# ============================================================================
# Railway Configuration
# ============================================================================

[build]
builder = "NIXPACKS"

[deploy]
# Health check endpoint (optional - WebSocket apps might not have one)
# healthcheckPath = "/"
# healthcheckTimeout = 300
# restartPolicyType = "ON_FAILURE"
# restartPolicyMaxRetries = 10
